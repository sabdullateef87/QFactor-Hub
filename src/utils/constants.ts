export enum Status {
  FAILURE = "FAILURE",
  SUCCESS = "SUCCESS"
}

export enum HttpResponseCode {
  BAD_REQUEST = 400,
  NOT_FOUND = 404,
  DUPLICATE_RECORD = 409,
  SUCCESS_OK = 200
}

export enum ResponseCode {
  BAD_REQUEST = 91,
  NOT_FOUND = 25,
  DUPLICATE_RECORD = 4
}

export enum ResponseMessage {
  NOT_FOUND = "not found"
}